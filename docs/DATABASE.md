# Database & auth

**Current approach:** Building without auth. Skip RLS (Row Level Security) policies for now.

**Before launch:** Add auth and RLS policies so data is scoped per user and access is enforced at the database layer.
